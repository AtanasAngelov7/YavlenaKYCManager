# Copy this file to .env and enter the RMS account credentials.
RMS_EMAIL=your-account@example.com
RMS_PASSWORD=replace-with-your-password

# Optional OpenAI-assisted property extraction. The application remains fully
# usable with the standard local parser when these values are absent.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
